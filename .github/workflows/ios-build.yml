name: Build Example App for iOS

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  ios-build:
    name: Build Example App on iOS (${{ matrix.platform }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    strategy:
      matrix:
        include:
          - platform: ios
            runner: macos-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: 'npm'
      
      - name: Install component dependencies
        run: npm ci
      
      - name: Install example app dependencies
        working-directory: example
        run: npm ci
      
      - name: Install CocoaPods
        if: matrix.platform == 'ios'
        working-directory: example/ios
        run: |
          sudo gem install cocoapods
          pod install
      
      # - name: Cache CocoaPods
      #   if: matrix.platform == 'ios'
      #   uses: actions/cache@v4
      #   with:
      #     path: |
      #       ~/.cocoapods
      #       example/ios/Pods
      #     key: ${{ runner.os }}-pods-${{ hashFiles('**/Podfile.lock') }}
      #     restore-keys: |
      #       ${{ runner.os }}-pods-
      
      # - name: Cache iOS Build
      #   if: matrix.platform == 'ios'
      #   uses: irgaly/xcode-cache@v1
      #   with:
      #     deriveddata-directory: xcode-derived-data
      #     key: xcode-cache-deriveddata-${{ github.workflow }}-${{ github.sha }}
      #     restore-keys: xcode-cache-deriveddata-${{ github.workflow }}-
      
      - name: Build iOS app
        if: matrix.platform == 'ios'
        working-directory: example/ios
        run: |
          rm ./example/ios/.xcode.env.local
          set -o pipefail
          xcodebuild -workspace example.xcworkspace \
                     -scheme example \
                     -sdk iphonesimulator \
                     -configuration Debug \
                     -derivedDataPath ../../xcode-derived-data \
                     build | xcpretty